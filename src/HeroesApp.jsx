import { AppRouter } from "./router/AppRouter";

export const HeroesApp = () => {
  // Definimos las rutas en otro directorio, componente llamado router
  return (
    <>
      <AppRouter>
      </AppRouter>
    </>
  );
};
